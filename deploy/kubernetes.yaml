apiVersion: apps/v1
kind: Deployment
metadata: {name: ai-evaluation-framework}
spec:
 replicas: 2
 selector: {matchLabels: {app: ai-evaluation-framework}}
 template:
  metadata: {labels: {app: ai-evaluation-framework}}
  spec:
   containers:
   - name: api
     image: ai-evaluation-framework:latest
     ports: [{containerPort: 8000}]
     readinessProbe: {httpGet: {path: /health/ready, port: 8000}}
     livenessProbe: {httpGet: {path: /health/live, port: 8000}}
